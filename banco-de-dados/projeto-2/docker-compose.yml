version: "3.9"
    
services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: projeto2
      MYSQL_DATABASE: projeto2
      MYSQL_USER: projeto2
      MYSQL_PASSWORD: projeto2

volumes:
  db_data: {}